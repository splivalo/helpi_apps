import 'package:flutter/material.dart';

import 'package:helpi_app/features/chat/presentation/direct_chat_screen.dart';

/// Senior chat tab — opens direct conversation with Helpi (admin).
class ChatScreen extends StatelessWidget {
  const ChatScreen({super.key});

  @override
  Widget build(BuildContext context) => const DirectChatScreen();
}
